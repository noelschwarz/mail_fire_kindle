<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MailFireKindleApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MailFireKindle"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- Sign-In Activity (Launcher) -->
        <activity
            android:name=".ui.SignInActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Inbox Activity -->
        <activity
            android:name=".ui.InboxActivity"
            android:exported="false"
            android:parentActivityName=".ui.SignInActivity" />

        <!-- Message Detail Activity -->
        <activity
            android:name=".ui.MessageDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.InboxActivity" />

        <!-- Compose Activity -->
        <activity
            android:name=".ui.ComposeActivity"
            android:exported="false"
            android:parentActivityName=".ui.InboxActivity"
            android:windowSoftInputMode="adjustResize" />

        <!-- MSAL BrowserTabActivity for handling auth redirects -->
        <activity
            android:name="com.microsoft.identity.client.BrowserTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- 
                    Replace YOUR_PACKAGE_NAME with your actual package name (e.g., com.mailfirekindle.app)
                    Replace BASE64_SIGNATURE_HASH with your app's signature hash
                    
                    To get the signature hash, run:
                    keytool -exportcert -alias androiddebugkey -keystore ~/.android/debug.keystore | openssl sha1 -binary | openssl base64
                    
                    The default debug keystore password is: android
                -->
                <data
                    android:host="com.mailfirekindle.app"
                    android:path="/6z0a6730ge7qjEK2tTx2y23TlfU="
                    android:scheme="msauth" />
            </intent-filter>
        </activity>

    </application>

</manifest>

